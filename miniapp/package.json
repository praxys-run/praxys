{
  "name": "praxys-miniapp",
  "version": "0.2.0",
  "private": true,
  "description": "Praxys WeChat Mini Program (native + Skyline)",
  "scripts": {
    "sync-types": "node scripts/sync-types.cjs",
    "sync-i18n": "node scripts/sync-i18n.cjs",
    "sync-legal": "node scripts/sync-legal.cjs",
    "check-i18n": "node scripts/check-i18n.cjs",
    "pretypecheck": "npm run sync-types && npm run sync-i18n && npm run sync-legal && npm run check-i18n",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "miniprogram-api-typings": "^5.2.1",
    "typescript": "^5.6.3"
  }
}